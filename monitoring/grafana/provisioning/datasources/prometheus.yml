# Grafana datasource provisioning — points Grafana at the Prometheus
# service from docker-compose.monitoring.yml.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
